import random

def genSequence():
    sqc = random.randint(0, 65535)
    return str(hex(src))

def genRNKey():
    return "{}{}-{}{}".format(genSequence(), genSequence(), genSequence(), genSequence())